/* =========================================
   Nexora-Mail — Configuration de l'API
   =========================================
   BASE_VIDE  = le site parle au serveur qui le sert
               (ex. http://localhost:3000 en local,
               ou votre domaine Netlify/Vercel/Render avec le reverse proxy).
   Si votre API est hébergée ailleurs, remplacez par son URL,
   ex. window.NEXORA_API_BASE = "https://mon-api.com";
   ========================================= */

window.NEXORA_API_BASE = "";