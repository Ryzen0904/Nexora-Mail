/* =========================================
   Nexora-Mail — CLÉ API SECRÈTE EmailJS (local)
   =========================================
   🔒  CE FICHIER NE DOIT JAMAIS ÊTRE PUBLIÉ SUR GITHUB.
      Il est exclu via .gitignore (js/emailjs-secret.js).
      S'il est absent, l'envoi externe ne marchera pas.
   ========================================= */

window.NEXORA_EMAILJS_SECRET = {
  secretKey: "",
};